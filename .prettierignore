node_modules
dist
.next
coverage
out
test-results
playwright-report
next-env.d.ts
package-lock.json

# O CLAUDE.md fica de fora: o Prettier indenta em 2 espaços qualquer linha que
# siga uma lista, e isso transforma uma regra que vale para a seção inteira em
# continuação do último item — muda o que a regra diz. Formatação não pode
# reescrever a regra do projeto.
CLAUDE.md

# Mesmo motivo do CLAUDE.md: o DESIGN.md é regra, não código, e a reindentação
# do Prettier muda o alcance de uma regra escrita depois de uma lista.
DESIGN.md

# Referência do demo anterior, guardada como está. Não é compilada nem publicada.
docs/referencia/
